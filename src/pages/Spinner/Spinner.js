import React from "react";
import "./Spinner.css"; // Ajoutez un fichier CSS pour styliser le spinner

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;